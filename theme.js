export const theme = {
    foreground: "#000000",
    background: "#FFFFFF",
    primary: "#9DFF20",
    secondary: "#345C00",
    tertiary: "#F6F6F6",
    slate: "#1E2938",
    // Default palette colors provided by wordpress
    // Only using one color as an example
    // The default palette can be turned off for wordpress by setting
    // "defaultPalette": false
    // In the theme.json wordpress file if you do not want to support the default colors
    "pale-cyan-blue": "#8ED1FC"
}